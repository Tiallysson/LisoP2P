<Window x:Class="LisoP2P.App.MainWindow"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
        xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
        xmlns:conv="clr-namespace:LisoP2P.App.Converters"
        xmlns:views="clr-namespace:LisoP2P.App.Views"
        mc:Ignorable="d"
        Title="LisoP2P" Height="680" Width="1100" MinWidth="820" MinHeight="480"
        Background="{StaticResource Surface2}">
    <Window.Resources>
        <conv:BoolToVisibilityConverter x:Key="BoolToVis" />
        <conv:SeverityToBrushConverter x:Key="SeverityToBrush" />
    </Window.Resources>

    <Grid>
        <Grid.RowDefinitions>
            <RowDefinition Height="*" />
            <RowDefinition Height="Auto" />
        </Grid.RowDefinitions>

        <Grid Grid.Row="0">
        <Grid.ColumnDefinitions>
            <ColumnDefinition Width="56" />
            <ColumnDefinition Width="184" />
            <ColumnDefinition Width="*" MinWidth="320" />
            <ColumnDefinition Width="Auto" />
        </Grid.ColumnDefinitions>

        <views:RoomRailView Grid.Column="0" />
        <views:RoomSidebarView Grid.Column="1" />
        <views:ChatView Grid.Column="2" DataContext="{Binding ActiveConversation}" />

        <views:MemberPanelView Grid.Column="3" Width="168"
                               Visibility="{Binding IsMemberPanelVisible, Converter={StaticResource BoolToVis}}" />

        <ItemsControl x:Name="Notifications" Grid.Column="2" Grid.ColumnSpan="2"
                      Panel.ZIndex="10"
                      VerticalAlignment="Top" Margin="12,10,12,0">
            <ItemsControl.ItemTemplate>
                <DataTemplate>
                    <Border CornerRadius="6" Padding="12,8" Margin="0,0,0,6"
                            Background="{Binding Severity, Converter={StaticResource SeverityToBrush}}"
                            BorderBrush="{StaticResource BorderSubtle}" BorderThickness="1">
                        <Grid>
                            <Grid.ColumnDefinitions>
                                <ColumnDefinition Width="*" />
                                <ColumnDefinition Width="Auto" />
                            </Grid.ColumnDefinitions>

                            <TextBlock Grid.Column="0" Text="{Binding Message}" Style="{StaticResource TextSmall}"
                                       Foreground="{StaticResource TextPrimary}"
                                       TextWrapping="Wrap" VerticalAlignment="Center" />

                            <Button Grid.Column="1" Style="{StaticResource FlatButton}"
                                    Content="{Binding ActionLabel}" Command="{Binding Action}"
                                    Margin="10,0,0,0"
                                    Visibility="{Binding HasAction, Converter={StaticResource BoolToVis}}" />
                        </Grid>
                    </Border>
                </DataTemplate>
            </ItemsControl.ItemTemplate>
        </ItemsControl>
        </Grid>

        <Border Grid.Row="1" Background="{StaticResource Surface0}" Padding="12,3">
            <TextBlock Text="{Binding StatusText}" Style="{StaticResource TextTiny}" />
        </Border>
    </Grid>
</Window>
